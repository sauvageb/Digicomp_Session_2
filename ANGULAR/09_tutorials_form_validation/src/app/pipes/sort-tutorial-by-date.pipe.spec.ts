import { SortTutorialByDatePipe } from './sort-tutorial-by-date.pipe';

describe('SortTutorialByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortTutorialByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
